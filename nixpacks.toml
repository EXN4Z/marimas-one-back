[phases.setup]
nixPkgs = ["php83", "php83Extensions.bcmath", "php83Extensions.gd"]

[start]
cmd = "php artisan migrate --force && php artisan config:clear && php artisan route:clear && php artisan view:clear && php artisan serve --host=0.0.0.0 --port=$PORT"